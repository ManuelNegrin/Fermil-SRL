# Fermil Platform Console

Aplicacion independiente para superadministradores. Configurar `VITE_API_URL`
con la URL publica del backend y agregar el dominio del deploy a
`CORS_ALLOWED_ORIGINS` en el backend.

```bash
npm run dev
npm run build
```
